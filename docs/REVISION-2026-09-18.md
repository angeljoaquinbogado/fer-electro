# Revisión técnica — 18/09/2026

## Cambios de este paquete

- Separación del CSS final en capas claras: `site.css`, `desktop.css`, `mobile.css` y `checkout-address.css`.
- Aumento de legibilidad en PC/notebook sin agrandar títulos principales.
- Capa `admin-desktop.css` para hacer más legible el panel en escritorio.
- Corrección del rango 901–980 px para no quedar sin navegación.
- Preload del fondo WebP del hero.
- Google Fonts del Admin pasa de `@import` a `preconnect` + `<link>`.
- Email de contacto visible en tienda, políticas y `security.txt`.
- Documentación actualizada de Resend a Gmail SMTP.
- Timeouts explícitos del SMTP para no retener el webhook indefinidamente.
- Imágenes de tabla del Admin con lazy-load/decodificación asíncrona.
- Eliminación de PNG pesados que ya tenían reemplazo WebP, conservando mapeos de compatibilidad para nombres/rutas antiguas.
- SEO enriquecido con `og:locale`, nombre del sitio, email y área servida.

## Deliberadamente no cambiado

- Credenciales de Mercado Pago: siguen como estaban.
- Esquema actual de Supabase: no requiere ejecutar SQL por estos cambios.
- Lógica del autocompletado de dirección: se conserva el comportamiento que ya fue aprobado.
